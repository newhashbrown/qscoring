# OS
.DS_Store
Thumbs.db
desktop.ini

# Editor
.vscode/
.idea/
*.swp
*~

# Office temp / lock files
~$*

# Env / secrets
.env
.env.*
!.env.example

# Build / deps
node_modules/
dist/
build/
.next/
.cache/

# Logs
*.log
npm-debug.log*

# Next.js
/out/
next-env.d.ts
*.tsbuildinfo

# Vercel
.vercel

# OpenNext
.open-next/
.wrangler/

# Research harness (offline) — generated panels + Python venv
research/data/
research/.venv/
# Python venvs for the batch jobs (factor exposures, freshness sentinel)
scripts/**/.venv/
__pycache__/
.pytest_cache/

# Local design/scratch artifacts (Stitch screenshots, dev logs)
.stitch/

# clerk configuration (can include secrets)
/.clerk/

# Personal / sensitive documents — identity, tax, registration. NEVER commit.
# (These were found untracked in a working copy; ignoring them keeps a stray
#  `git add -A` from ever staging private business/tax paperwork.)
/BusinessRegistrationCertificate.pdf
/CP_575_G.pdf
/deptoftreasury-letter.jpg
# Defensive globs for the same categories anywhere in the tree:
*CP_575*
*RegistrationCertificate*
*deptoftreasury*

# Claude Code local (machine-specific, never commit)
CLAUDE.local.md
policy-sample-output.json
policy-batch-output.json
policy-batch-upserts.sql
